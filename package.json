{
  "name": "web3happened-docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p out && cp -r public/. out/",
    "dev": "python3 -m http.server 3001 --directory public"
  },
  "engines": {
    "node": ">=20"
  }
}
